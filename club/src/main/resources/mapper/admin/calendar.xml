<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="calendar">
	<select id="select" resultType="calendar.CalendarVO" parameterType="String">
		select today, name from calendar 
    	where today LIKE '${yearmonth}%' 
    	order by today asc
	</select>
	
	<insert id="insert" parameterType="calendar.CalendarVO">
		insert into project.schedule
		(spot_num, date, title, contents, writer, member_id, regdate)
		values  
		(#{spot_num},#{date},#{title},#{contents},#{writer},#{member_id}, now()) 
	</insert> 
	 
</mapper>

