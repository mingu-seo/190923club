<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="file">

	<!-- 파일선택 -->
	<select id="file_select" parameterType="notice.NoticeVO">
		select * from project.reply where post_id=#{post_id} and board_id=#{board_id}
	</select>

	<insert id="file_insert" parameterType="reply.ReplyVO">
		insert into project.reply (contents, regdate, post_id, board_id, g_id, g_lev, g_seq) 
		values(#{contents}, now(), #{post_id}, #{board_id}, #{g_id}, #{g_lev}, #{g_seq})
	</insert>
	
</mapper>