<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="like">

	<!-- 좋아요 인서트 -->
	<insert id="like_insert" parameterType="like.LikeVO">
		insert into project.like (post_id, board_id, member_id) 
		values(#{post_id}, #{board_id}, #{member_id})
	</insert>
	
	<!-- 좋아요 취소 -->
	<delete id="like_delete" parameterType="like.LikeVO">
		delete from project.like where post_id=#{post_id} and board_id=#{board_id} and member_id=#{member_id}
	</delete>
	
</mapper>